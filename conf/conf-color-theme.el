;;----------------------------------------------------------------------------;;
;; Color Settings
;; Emacsの配色をまとめて設定
;;----------------------------------------------------------------------------;;
(require 'color-theme)
(eval-after-load "color-theme"
  '(progn
     (color-theme-initialize)
     (color-theme-clarity)
     ))
;(color-theme-dark-laptop)
;;----------------------------------------------------------------------------;;