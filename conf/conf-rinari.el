(setq rinari-minor-mode-prefixes (list ";"))
(require 'rinari)

;; (add-hook 'ruby-mode-hook
;;           '(lambda ()
;; 	     (local-set-key (kbd "C-;") 'rinari-find-by-context)
;; ))

;; 	     (global-set-key (kbd "C-;") 'rinari-find-by-context)
